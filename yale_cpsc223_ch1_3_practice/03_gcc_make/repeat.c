#include <stdio.h>
#include <stdlib.h>

int main(int argc, char **argv) {
    /* TODO:
       - require exactly two arguments after the program name
       - argv[1] is the word
       - argv[2] is the count
       - print the word count times, one per line
       - return nonzero for invalid input
    */
    (void)argc;
    (void)argv;
    return 0;
}
